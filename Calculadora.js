var num;
var operador;
const readLineSync = require(readLineSync);
const operador = readLineSync.question("Insira a Operacão que Deseja Efetuar (+) (-) (*) (/)? : \n");
const num = parseFloat(reaLineSync.question("Insira o Numero: \n"));
